- id: ${ provider }
  provider: ${ provider }
  mapper_url: ${ mapper_url }
  client_id: ${ client_id }
  client_secrret: ${ client_secret }
  tenant: ${ tenant }
  scope:
    # TODO: Make this scope match the provider
    - user:email